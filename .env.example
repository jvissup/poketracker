# Database — get a free Neon Postgres URL at https://neon.tech
# Or use any Postgres provider (Supabase, Railway, etc.)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Pokemon TCG API — optional but raises rate limits significantly
# Get a free key at https://dev.pokemontcg.io
POKEMON_TCG_API_KEY=""
